import { RagQueryView } from './components/RagQueryView';

/**
 * Home route — client RAG search UI (state and /api/search live in RagQueryView).
 */
export default function Home() {
  return <RagQueryView />;
}
